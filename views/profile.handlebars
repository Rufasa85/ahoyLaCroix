<h1>{{username}}</h1>
<p>{{bio}}</p>
    <h3>Loves</h3>
<div class="canBox">
    {{#each Love}}
        <img src="{{pic}}" alt="{{name}} can of la croix">
    {{/each}}
</div>
    <h3>Hates</h3>
<div class="canBox">
    {{#each Hate}}
        <img src="{{pic}}" alt="{{name}} can of la croix">
    {{/each}}
</div>